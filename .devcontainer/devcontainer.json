{
  "name": "jazzy desktop",
  "build": { "dockerfile": "Dockerfile" },
  "remoteUser": "root",
  "runArgs": [
    "--runtime", "nvidia",
    "--network=host",
    "-e", "DISPLAY=${env:DISPLAY}"
  ],
  "mounts": [
    "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind"
  ]
}
